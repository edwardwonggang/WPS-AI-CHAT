请生成一段用于 WPS 正文渲染回归测试的 Markdown 文档。请只输出正文内容，不要寒暄、不要解释。

必须包含：
1. 一个一级标题、一个二级标题、一个三级标题。
2. 两段中文正文。第一段正文必须原样包含这一整句，不要删除任何星号或反引号：This sentence contains **bold text**, *italic text*, and `inlineCode`.
3. 一个 3 列 4 行 Markdown 表格，表头为“项目 / 标准 / 检查点”。
4. 一个 ```js 代码块，代码里必须包含注释、const、function、if、return。
5. 一个引用块，引用块内容必须以“> 引用说明：”开头。
6. 一个有序列表和一个无序列表。

内容主题：论文写作格式检查示例。
