请生成一段用于 WPS 正文渲染回归测试的 Markdown 硕士论文骨架。请只输出正文内容，不要寒暄、不要解释。

必须覆盖以下结构和格式，标题文字尽量原样保留：

1. 无编号前置部分：
   - `# 摘要`
   - 一段中文摘要正文，包含“关键词：WPS；硕士论文；格式渲染”。
   - `# Abstract`
   - 一段英文摘要正文，包含“Keywords: WPS; thesis; rendering”。

2. 正文章节：
   - `# 1 绪论`
   - `## 研究背景`（不要手写编号，系统应自动生成 1.1）
   - `### 1.1.1 研究问题`（这里故意手写编号，用来检查系统不要重复编号）
   - `### 研究意义`（不要手写编号，系统应自动生成 1.1.2）
   - `# 2 系统设计`
   - `## 2.1 渲染链路`（这里故意手写编号，用来检查系统不要重复编号）

3. 正文段落：
   - 至少 4 段中文正文。
   - 第一段正文必须原样包含这一整句，不要删除任何星号或反引号：This sentence contains **bold text**, *italic text*, and `inlineCode`.

4. 学术正文常见元素：
   - 一个 3 列 5 行 Markdown 表格，表头为“项目 / 标准 / 检查点”。
   - 一个以“图1 系统渲染流程示意”开头的图题段落。
   - 一个以“表1 论文格式检查项”开头的表题段落。
   - 一个块级公式：`$$E=mc^2$$`。
   - 一个 ```js 代码块，代码里必须包含注释、const、function、if、return。
   - 一个引用块，引用块内容必须以“> 引用说明：”开头。
   - 一个有序列表和一个无序列表。

5. 后置部分：
   - `# 参考文献`
   - 至少 2 条参考文献，其中一条包含“GB/T 7713.1-2025”。
   - `# 致谢`
   - 一段致谢文字。

关键要求：
- 不要给“摘要、Abstract、参考文献、致谢”额外添加数字编号。
- 已经手写编号的标题不要再重复编号。
- 内容主题：硕士论文格式渲染标准回归测试。
